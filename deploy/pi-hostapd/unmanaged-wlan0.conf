# Install as /etc/NetworkManager/conf.d/unmanaged-wlan0.conf. Raspberry Pi
# OS (Bookworm+) uses NetworkManager by default, not dhcpcd -- this stops it
# fighting hostapd for control of wlan0. Apply with:
#   sudo systemctl reload NetworkManager
[keyfile]
unmanaged-devices=interface-name:wlan0
